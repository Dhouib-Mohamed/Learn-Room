import {Box, Heading} from '@chakra-ui/react';

const Header = () => {
    return (
        <Box bg="blue.500" w="100%" p={4} color="white">
            <Heading>Google Classroom Clone</Heading>
        </Box>
    );
};

export default Header;